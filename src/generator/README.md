# Generator Module

Responsible for build-time type introspection and schema construction.

Planned files:
- TypeParser.ts: Parses TypeScript AST/types.
- DecoratorProcessor.ts: Applies class-based decorator metadata.
- SchemaBuilder.ts: Combines parsed types + overrides into FormSchema.
